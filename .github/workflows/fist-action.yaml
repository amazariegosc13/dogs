name: Fetch API and Commit

on:
  workflow_dispatch: 
  schedule:
   - cron: '15 4,12,21 * * *' 

 
permissions:
  contents: write

jobs:
  update-json:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout  
        uses: actions/checkout@v4

      - name: Fetch
        run: |
          sleep $(( RANDOM % 60 ))
          curl -s "https://dogapi.dog/api/v1/facts?number=5" -o data.json

      - name: Push
        run: |
          git config --global user.name "amazariegosc13"
          git config --global user.email "amazariegosc13@miumg.edu.gt"
          
          git add data.json
           
          if git diff --staged --quiet; then
            echo "No changes."
          else
            git commit -m "New Dogs Data"
            git push
          fi
